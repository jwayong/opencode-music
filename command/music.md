---
description: Toggle Apple Music playback while opencode works (on | off | toggle | status)
---

Manage background music that plays while opencode is working.

Call the `apple_music` tool with `action` set to `$ARGUMENTS`. If `$ARGUMENTS` is empty, use `status`.

Do not do anything else — just invoke the tool once and report its one-line result back to me.
